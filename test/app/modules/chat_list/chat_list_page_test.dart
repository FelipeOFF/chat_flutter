import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:chat/app/modules/chat_list/chat_list_page.dart';

void main() {
  testWidgets('ChatListPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ChatListPage(title: 'ChatList')));
    //  final titleFinder = find.text('ChatList');
    //  expect(titleFinder, findsOneWidget);
  });
}
